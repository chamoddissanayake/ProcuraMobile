import React, { Component } from "react";
import { Text, StyleSheet, View } from "react-native";

export default class OrderPlacedComponent extends Component {
  render() {
    return (
      <View>
        <Text> Order Placed Component </Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});
