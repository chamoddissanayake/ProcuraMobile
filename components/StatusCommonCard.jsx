import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class StatusCommonCard extends Component {
    render() {
        return (
            <View>
                <Text> Status Common card </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
