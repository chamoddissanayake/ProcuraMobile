import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class PendingCard extends Component {
    render() {
        return (
            <View>
                <Text> Pending card </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
